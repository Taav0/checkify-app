import { LocationChangeEvent } from '@angular/common';

export class Product {

    id: number;
    barcode: string;
    name: string;
    expireDate: Date;
    description: string;
    imageUrl: string;
}
