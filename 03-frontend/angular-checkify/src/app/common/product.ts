import { LocationChangeEvent } from '@angular/common';

export class Product {

    id: string;
    barcode: string;
    name: string;
    date: Date;
    description: string;
    imageUrl: string;
    productCategory: string;
}